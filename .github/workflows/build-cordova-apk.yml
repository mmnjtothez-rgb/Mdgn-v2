name: Build Cordova WebView APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Vite dependencies
        run: npm install

      - name: Build Vite project
        run: npm run build

      - name: Install Cordova
        run: npm install -g cordova

      - name: Create Cordova project
        run: |
          cordova create cordova-app com.mdgn.app mdgnapp
          cd cordova-app
          cordova platform add android

      - name: Copy built Vite app into Cordova
        run: |
          rm -rf cordova-app/www/*
          cp -r dist/* cordova-app/www/

      - name: Build APK
        run: |
          cd cordova-app
          cordova build android --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: mdgn-cordova-apk
          path: cordova-app/platforms/android/app/build/outputs/apk/release/app-release.apk
